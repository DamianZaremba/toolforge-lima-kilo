---
- name: Install a kind-based fake Toolforge in a Debian-based machine
  hosts: localhost
  vars:
    target: install
  vars_files:
    - vars/kind.yaml
    - vars/toolforge.yaml
    - vars/k8s.yaml
  roles:
    - { role: toolforge }
    - { role: kind }
    - { role: kubectl }
    - { role: k8s }
