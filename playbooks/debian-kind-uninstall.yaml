---
- name: Uninstall a kind-based fake Toolforge in a Debian-based machine
  hosts: localhost
  vars:
    target: uninstall
  vars_files:
    - vars/lima-kilo.yaml
    - vars/kind.yaml
    - vars/toolforge.yaml
  roles:
    - { role: userconfig }
    - { role: kind }
    - { role: toolforge }
    - { role: kubectl }
    - { role: k8s }
    - { role: apt }
